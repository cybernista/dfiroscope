<UserControl x:Class="ProcInsider.Views.Features.Ai.AiChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,4"
                Padding="6"
                Background="#FFFFFFFF"
                BorderBrush="#FFD8D8D8"
                BorderThickness="1">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="Reload"
                        Command="{Binding ReloadSettingsCommand}"
                        Padding="8,2"
                        Margin="6,0,0,0"/>
                <TextBlock Text="{Binding ProviderStatusDisplay}"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1"
                Background="#FFFFF6DF"
                BorderBrush="#FFE0B95C"
                BorderThickness="1"
                Padding="6"
                Margin="0,0,0,4"
                Visibility="{Binding IsCloudProvider, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding PrivacyWarning}"
                       TextWrapping="Wrap"
                       Foreground="#664400"/>
        </Border>

        <Border Grid.Row="2"
                BorderBrush="#FFD8D8D8"
                BorderThickness="1"
                Background="#FFFFFFFF"
                Margin="0,0,0,4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="6">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#FFE0E0E0"
                                    BorderThickness="0,0,0,1"
                                    Padding="0,0,0,6"
                                    Margin="0,0,0,6">
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,2">
                                        <TextBlock DockPanel.Dock="Right"
                                                   Text="{Binding CreatedDisplay}"
                                                   Foreground="#777777"
                                                   FontSize="10"/>
                                        <TextBlock Text="{Binding AuthorDisplay}" FontWeight="SemiBold"/>
                                    </DockPanel>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding ProviderDisplay}"
                                               Foreground="#777777"
                                               FontSize="10"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding PromptText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     MinHeight="70"
                     MaxHeight="120"
                     Padding="4"
                     FontSize="12"/>
            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                <Button Content="Send" Command="{Binding SendMessageCommand}" Padding="10,4" Margin="0,0,0,4"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="10,4" Margin="0,0,0,4"/>
                <Button Content="Clear" Command="{Binding ClearConversationCommand}" Padding="10,4"/>
            </StackPanel>
        </Grid>

        <DockPanel Grid.Row="4">
            <ProgressBar DockPanel.Dock="Left"
                         Width="70"
                         Height="14"
                         IsIndeterminate="{Binding IsBusy}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                         Margin="0,2,6,0"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#555555" TextWrapping="Wrap"/>
        </DockPanel>
    </Grid>
</UserControl>
