<UserControl x:Class="ProcInsider.Views.Features.Artifacts.DataMemoryDumpsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Refresh Dumps"
                    Command="{Binding RefreshMemoryDumpsCommand}"
                    Padding="10,3" Margin="0,0,10,0"/>
            <Button Content="Queue Full Dump"
                    Command="{Binding DataContext.QueueSelectedProcessDumpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Padding="10,3" Margin="0,0,10,0"/>
            <Button Content="Analyze Dump PE"
                    Command="{Binding DataContext.AnalyzeSelectedDumpPeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Padding="10,3" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding MemoryDumpsView}"
                  SelectedItem="{Binding SelectedMemoryDump, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  Margin="5"
                  FontSize="11">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Requested" Binding="{Binding RequestedDisplay}" Width="135"
                                    SortMemberPath="RequestedUtc"/>
                <DataGridTextColumn Header="Kind" Binding="{Binding DumpKind}" Width="70"
                                    SortMemberPath="DumpKind"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"
                                    SortMemberPath="Status"/>
                <DataGridTextColumn Header="Size" Binding="{Binding FileSizeDisplay}" Width="85"
                                    SortMemberPath="FileSizeBytes"/>
                <DataGridTextColumn Header="Path" Binding="{Binding FilePath}" Width="300"
                                    SortMemberPath="FilePath">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding FilePath}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="SHA256" Binding="{Binding Sha256Hash}" Width="220"
                                    SortMemberPath="Sha256Hash">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding Sha256Hash}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="FontFamily" Value="Consolas"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="260"
                                    SortMemberPath="ErrorMessage">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding ErrorMessage}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Margin="5,0,5,5">
            <Run Text="Dumps: "/>
            <Run Text="{Binding MemoryDumps.Count, Mode=OneWay}" FontWeight="Bold"/>
        </TextBlock>
    </Grid>
</UserControl>
