<UserControl x:Class="ProcInsider.Views.Explorer.ExploreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,4"
                Padding="6"
                Background="#FFFFFFFF"
                BorderBrush="#FFD8D8D8"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding SelectedScopeTitle}"
                           FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding SelectedScopeDescription}"
                           Margin="0,3,0,0"
                           Foreground="#555555"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <TreeView Grid.Row="1"
                  ItemsSource="{Binding RootNodes}"
                  SelectedItemChanged="ExplorerTree_SelectedItemChanged"
                  PreviewMouseLeftButtonDown="ExplorerTree_PreviewMouseLeftButtonDown"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  BorderBrush="#FFD8D8D8"
                  BorderThickness="1">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <EventSetter Event="Expanded" Handler="ExplorerTreeItem_Expanded"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>
