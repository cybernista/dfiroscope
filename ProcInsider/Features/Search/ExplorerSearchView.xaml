<UserControl x:Class="ProcInsider.Features.Search.ExplorerSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,4" LastChildFill="True">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="6,0,0,0">
                <Button Content="Search"
                        Command="{Binding SearchCommand}"
                        Padding="8,2" Margin="0,0,4,0"/>
                <Button Content="Open"
                        Command="{Binding OpenResultCommand}"
                        Padding="8,2"/>
            </StackPanel>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Height="24"
                     Padding="4,2"
                     VerticalContentAlignment="Center"
                     KeyDown="SearchTextBox_KeyDown"/>
        </DockPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,4">
            <CheckBox Content="Processes" IsChecked="{Binding IncludeProcesses, Mode=TwoWay}" Margin="0,0,8,3"/>
            <CheckBox Content="Events" IsChecked="{Binding IncludeEvents, Mode=TwoWay}" Margin="0,0,8,3"/>
            <CheckBox Content="Modules" IsChecked="{Binding IncludeModules, Mode=TwoWay}" Margin="0,0,8,3"/>
            <CheckBox Content="Handles" IsChecked="{Binding IncludeHandles, Mode=TwoWay}" Margin="0,0,8,3"/>
            <CheckBox Content="Unresolved / ambiguous" IsChecked="{Binding IncludeCorrelationEvidence, Mode=TwoWay}" Margin="0,0,8,3"/>
        </WrapPanel>

        <Grid Grid.Row="2">
            <TextBlock Margin="10"
                       Text="{Binding StatusMessage}"
                       Foreground="#666666"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <DataGrid ItemsSource="{Binding ResultsView}"
                      SelectedItem="{Binding SelectedResult, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      EnableRowVirtualization="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Margin="0"
                      FontSize="11"
                      MouseDoubleClick="SearchResultsDataGrid_MouseDoubleClick"
                      Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="60"/>
                    <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="95"/>
                    <DataGridTextColumn Header="State" Binding="{Binding CorrelationState}" Width="85"/>
                    <DataGridTextColumn Header="Match" Binding="{Binding MatchedValue}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="ToolTip" Value="{Binding MatchedValue}"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <DockPanel Grid.Row="3" Margin="0,4,0,0">
            <Button DockPanel.Dock="Right"
                    Content="Clear"
                    Command="{Binding ClearCommand}"
                    Padding="8,2"/>
            <ProgressBar Width="70"
                         Height="14"
                         Margin="0,4,8,0"
                         IsIndeterminate="{Binding IsSearching}"
                         Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </DockPanel>
    </Grid>
</UserControl>
