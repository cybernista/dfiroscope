<Window x:Class="ProcInsider.AuditApplyResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply audit settings" Width="800" Height="600" MinWidth="560" MinHeight="380"
        WindowStartupLocation="CenterOwner">
    <Window.Resources><BooleanToVisibilityConverter x:Key="VisibleWhenTrue"/></Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="{Binding Summary}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <ScrollViewer x:Name="ResultsScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border Background="#FFF4DE" Padding="10" Margin="0,0,0,10" Visibility="{Binding HasAttention, Converter={StaticResource VisibleWhenTrue}}">
                    <TextBox Text="{Binding Attention, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" TextWrapping="Wrap"/>
                </Border>
                <ItemsControl x:Name="ActionResults" ItemsSource="{Binding Actions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="0,10">
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,5">
                                        <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontWeight="Bold" Width="100"/>
                                        <TextBlock Text="{Binding Action}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    </DockPanel>
                                    <TextBox Text="{Binding Message, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" TextWrapping="Wrap"/>
                                    <Expander Header="Technical details" Margin="0,5,0,0" Visibility="{Binding HasTechnicalDetails, Converter={StaticResource VisibleWhenTrue}}">
                                        <TextBox Text="{Binding TechnicalDetails, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" TextWrapping="Wrap" Margin="8"/>
                                    </Expander>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="Saved backup" FontWeight="SemiBold" Margin="0,16,0,4"/>
                <TextBox Text="{Binding BackupPath, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
        <Button x:Name="CloseButton" Grid.Row="2" Content="Close" IsCancel="True" Width="90" HorizontalAlignment="Right" Margin="0,12,0,0" Padding="8,4"/>
    </Grid>
</Window>
